<h1>Bands</h1>
<ul>
    <% Band.all.order(:name).each do |band| %>
        <li>
            <%= link_to band.name, band_url(band) %>
        </li>
    <% end %>
</ul>

<%= link_to "Create a New Band", new_band_url %>